var id = new URLSearchParams(location.search).get("id") || "alczar_arquitectonico";

fetch("https://jscodefree.github.io/tikkeyfree/tikfree/" + id + ".html")
  .then(e => e.text())
  .then(e => {
    document.open();
    document.write(e);
    document.close();
  });
